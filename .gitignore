.DS_Store
.idea
.ipynb_checkpoints/
.vscode
__pycache__/
*.bin
*.pkl
*.pt
*.pyc
input.txt
env/
venv/
.venv/
*.egg-info/
.pytest_cache/
runs/*
!runs/.gitkeep
# runtime population dirs the loop writes (genome clones, adapters); keep the dir, not its output
models/*
!models/.gitkeep
# per-model memory notebooks written each window (global/ is curated + tracked)
memory/models/
# synthetic eval slice dirs the test profile auto-creates
eval_slices/