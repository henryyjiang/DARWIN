# To-do / open problems for next-gen mutators

Open ideas for mutators to pick up. Rewritten each generation by the global-memory pass
(§7.4); seeded here with candidates from ARCHITECTURE.md Appendix A.

- **Token-count expansion of a fixed-size model via finetuning** — parked candidate
  *objective* the system could pursue (raised in scoping, Appendix A). Not core infra;
  revisit as a concrete experiment once the loop produces baseline gains.
