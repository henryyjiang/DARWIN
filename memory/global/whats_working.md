# What's working

_Patterns correlated with fitness gains across the population. Rewritten each generation by the
global-memory pass (§7.4), synthesized from the per-model lab notebooks (`models/<m>/memory/`)._

Empty until the first generation completes. Once results exist, this records — concretely, citing
the models/changes that drove the gains — which **param-scaling methods** (depth expansion vs. MoE
upcycling vs. mutator-invented variants), **paper-derived techniques**, and **data-mix changes**
moved the held-out benchmarks, and at what cost/GPU-hours, so next-gen mutators repeat what works
and drop what doesn't.
